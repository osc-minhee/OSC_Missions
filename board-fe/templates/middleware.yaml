apiVersion: traefik.io/v1alpha1  
kind: Middleware
metadata:
  name: https-redirect
  labels:
    {{- include "board-fe.labels" . | nindent 4}}
spec:
  redirectScheme:
    scheme: https
    permanent: true