apiVersion: v1
kind: Service
metadata:
  name: {{ include "board-fe.fullname" . }}
  labels:
    {{- include "board-fe.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "board-fe.selectorLabels" . | nindent 4 }}
