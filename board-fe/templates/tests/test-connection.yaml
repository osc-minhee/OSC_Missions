apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "board-fe.fullname" . }}-test-connection"
  labels:
    {{- include "board-fe.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "board-fe.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
